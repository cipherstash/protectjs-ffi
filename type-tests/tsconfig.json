{
  "//": "Type tests for the PATCHED wasm .d.ts. Separate from the root tsconfigs because these need `npm run build:wasm` to have run — `npm test` must still pass in a fresh clone with no dist/. Run via `npm run test:typecheck:wasm`.",
  "extends": "@tsconfig/node20/tsconfig.json",
  "compilerOptions": {
    "module": "node16",
    "moduleResolution": "node16",
    "noEmit": true,
    "declaration": false,
    "strict": true
  },
  "include": ["**/*.mts"]
}
